# Remote-Server-Health-Monitoring
Bash-based tool for remote server health auditing via SSH. Ingests a host list, iterates over targets, executes system checks (CPU, memory, disk, uptime), parses outputs, and generates structured reports. Implements strict error handling, logging, and secure execution.
